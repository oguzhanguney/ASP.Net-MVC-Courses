--Web apide 2 çeişt route yapılanması vardır. birincisi convetion based routing digeri ise attribute based routing .
	convention based routing: "api/{controller}/{id}"
		web api config dosyasına gidelim.

--values controllera gidip bir örnek yapalım.

--action based routing için webapiconfigi düzenleyelim.